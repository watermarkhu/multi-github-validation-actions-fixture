name: ci

on:
  push:

permissions: {}

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8
        with:
          version: 11.2.2
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm test
      - run: pnpm run build
      - uses: ./report
        with:
          state: started
          app_id: ${{ vars.WATERMARKHU_APP_ID }}
          private_key: ${{ secrets.WATERMARKHU_APP_PRIVATE_KEY }}
      - uses: ./report
        with:
          state: success
          app_id: ${{ vars.WATERMARKHU_APP_ID }}
          private_key: ${{ secrets.WATERMARKHU_APP_PRIVATE_KEY }}
      - uses: ./report
        if: always() && failure()
        with:
          state: failure
          app_id: ${{ vars.WATERMARKHU_APP_ID }}
          private_key: ${{ secrets.WATERMARKHU_APP_PRIVATE_KEY }}
      - uses: ./report
        if: always() && cancelled()
        with:
          state: cancelled
          app_id: ${{ vars.WATERMARKHU_APP_ID }}
          private_key: ${{ secrets.WATERMARKHU_APP_PRIVATE_KEY }}

  zizmor:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Run zizmor 🌈
        uses: zizmorcore/zizmor-action@5f14fd08f7cf1cb1609c1e344975f152c7ee938d # v0.5.6
